import "./index.css"
function Loading() {
  return (
    <div className="loading">
        Loading...
        <p>If persists check your network or refresh the page</p>
    </div>
  );
}

export default Loading;